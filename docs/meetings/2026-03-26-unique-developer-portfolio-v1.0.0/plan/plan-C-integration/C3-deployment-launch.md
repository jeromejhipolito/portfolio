# C3: Deployment & Launch

**Plan:** Integration
**Depends on:** C2-testing-performance (all tests pass, scores >= 90)
**Verify before starting:** Lighthouse >= 90, zero broken links, E2E + a11y tests pass
**BRs covered:** BR-012, BR-014, BR-022, BR-024
**Estimated tasks:** 6

---

## Tasks

### C3.1: Vercel Production Deployment

**Type:** deployment
**Files:**
- `vercel.json` — verify: production config

**Implementation:**
Verify Vercel connection: production branch (main), preview on PRs, custom domain with HTTPS. Environment variables: RESEND_API_KEY, CONTACT_EMAIL, NEXT_PUBLIC_GIT_SHA (from Vercel system env). Verify: auto-deploy on push, preview URLs per PR, custom domain resolves with HTTPS, canonical URL matches.

**AC:**
- [ ] Push to main triggers production deploy
- [ ] PRs get preview URLs with noindex header
- [ ] Custom domain with HTTPS
- [ ] Env vars set (not in code)
- [ ] Deploy completes < 2 minutes

---

### C3.2: Finalize CI Pipeline

**Type:** CI/CD
**Files:**
- `.github/workflows/ci.yml` — modify: add all gates

**Implementation:**
Complete pipeline: checkout → cache → install → lint → typecheck → unit tests (Vitest) → build → Lighthouse CI (>= 90) → broken-link checker → E2E tests (Playwright) → a11y tests. Add gitleaks secrets scan. All steps block merge on failure. Total time target: < 5 minutes.

**AC:**
- [ ] All 9 steps run in sequence
- [ ] Pipeline blocks merge on any failure
- [ ] Gitleaks: zero secrets in codebase
- [ ] Total time < 5 minutes

---

### C3.3: Analytics with Custom Events

**Type:** analytics
**Files:**
- `src/app/layout.tsx` — modify: add Plausible script
- `next.config.js` — modify: update CSP for Plausible domain
- `src/lib/analytics.ts` — create: event tracking helpers

**Implementation:**
Add Plausible script (cloud or self-hosted). Custom events via `plausible()` function: `contact_submit`, `linkedin_click`, `github_click`, `project_view` (on project page load), `workflow_section_viewed` (on scroll). Create typed analytics helpers: `trackEvent(name, props?)`. Support UTM params natively (Plausible captures them). Update CSP connect-src for Plausible domain.

**AC:**
- [ ] Plausible dashboard shows page views
- [ ] Custom events fire for contact submit, LinkedIn click, GitHub click
- [ ] Project page views tracked per project
- [ ] UTM params captured from URL
- [ ] CSP allows Plausible domain

---

### C3.4: Uptime Monitoring

**Type:** monitoring
**Files:**
- Documentation only — external service setup

**Implementation:**
UptimeRobot (free tier): monitor GET /api/health every 5 minutes. Keyword check: response contains `"status":"ok"`. Alert via email on downtime. Monitor both production URL and custom domain.

**AC:**
- [ ] Monitor checks /api/health every 5 minutes
- [ ] Email alert within 5 minutes of downtime

---

### C3.5: CI Pipeline Showcase on Portfolio

**Type:** content
**Files:**
- `src/data/workflow.ts` — modify: Step 4 details
- `public/diagrams/ci-pipeline.svg` — create: pipeline diagram

**Implementation:**
SVG diagram of actual CI pipeline (lint → typecheck → test → build → Lighthouse → link-check → E2E → deploy). Add to Workflow Step 4 detail panel. Link to `.github/workflows/ci.yml` on GitHub. Diagram uses design token colors.

**AC:**
- [ ] Pipeline diagram renders in Workflow Step 4
- [ ] Diagram uses portfolio colors
- [ ] Link to actual workflow file works

---

### C3.6: Launch Checklist

**Type:** audit
**Files:**
- `LAUNCH_CHECKLIST.md` — create: verification doc

**Implementation:**
Final verification:
- [ ] All project GitHub links return 200
- [ ] Contact form e2e: submit → email received
- [ ] SecurityHeaders.com grade A or A+
- [ ] Google Rich Results Test passes (homepage + 2 project pages)
- [ ] Lighthouse mobile >= 90 on production URL
- [ ] robots.txt allows crawling (generated by next-sitemap)
- [ ] sitemap.xml generated and includes all project pages
- [ ] og:image renders correctly (test via opengraph.xyz)
- [ ] Favicon in multiple sizes
- [ ] 404 page exists with nav back to home
- [ ] All NDA-cleared projects verified (BR-017)
- [ ] Analytics tracking confirmed (page views + custom events)
- [ ] Uptime monitoring active
- [ ] Success metric baseline recorded (from P0.1)
- [ ] UTM-tagged URLs prepared: `?utm_source=linkedin`, `?utm_source=application`

**AC:**
- [ ] All checklist items verified and documented
- [ ] Production URL live and accessible
- [ ] 60-day measurement window begins
- [ ] PORTFOLIO IS LIVE

---

## Phase Checklist

- [ ] All 6 tasks implemented
- [ ] Production deployment on custom domain
- [ ] CI pipeline complete with all 9 gates
- [ ] Analytics tracking page views + custom events
- [ ] Uptime monitoring active
- [ ] Launch checklist 100% verified
- [ ] UTM URLs prepared for distribution
- [ ] PORTFOLIO IS LIVE
